pre-commit:
  parallel: true
  commands:
    lint:
      glob: "src/**/*.{ts,tsx}"
      run: npx oxlint {staged_files}
    format:
      glob: "src/**/*.{ts,tsx}"
      run: npx oxfmt --check {staged_files}
